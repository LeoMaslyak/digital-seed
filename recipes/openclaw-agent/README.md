# OpenClaw Agent Recipe

OpenClaw is useful when you want an always-on assistant with messaging, reminders, and local tools.

Safe initialization command:

```bash
bun run seed recipe openclaw init
```

This creates a draft context file only. Review before connecting external accounts.
