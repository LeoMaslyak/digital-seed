# Hermes Agent Recipe

Hermes-style agents are useful when you want a terminal-capable agent to execute setup steps and explain tradeoffs.

Safe initialization command:

```bash
bun run seed recipe hermes init
```

This creates a draft context file only. Review before granting credentials or messaging permissions.
